pipeline {
    agent any
    stages {
        stage('Build') { steps { echo 'Building Node.js app' } }
        stage('Test') { steps { echo 'Running Node.js tests' } }
        stage('Deploy') { steps { echo 'Deploying Node.js app' } }
    }
}